resources:
  apps:
    compliance_agent:
      name: gsk-compliance-agent-${bundle.target}
      description: >
        GSK FRMC Controls Evidence Review Agent — AI-powered compliance testing
        with parallel evidence review, confidence scoring, and audit-ready reports.
      source_code_path: ../deploy_app

      permissions:
        - level: CAN_USE
          group_name: users
